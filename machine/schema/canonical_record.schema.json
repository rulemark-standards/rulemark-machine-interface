{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "canonical_id": {
        "type": "string",
        "description": "Canonical identifier of the record, e.g. RM-S-REAL-001"
      }
    },
    "required": ["canonical_id"],
    "additionalProperties": true
  }
  