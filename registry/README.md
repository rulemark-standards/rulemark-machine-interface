# Derived automation registry

This directory is not an independent source of truth. It mirrors the public
frozen set for automation. The authoritative registry and release-integrity
baseline live in `rulemark-canonical-archive`.

Draft candidates must not use canonical publication paths and must never be
represented as registered, frozen, or publicly citable records.
